query fetchRequestById(
  $requestId: RequestIdentifierInputType!
  $customerLanguage: CustomerLanguage!
) {
  request(requestIdentifier: $requestId, customerLanguage: $customerLanguage) {
    ...requestFields
  }
}
